pipeline {
  agent any
  stages {
    stage('Deploy CloudFormation Stack') {
      steps {
          sh "aws cloudformation create-stack --stack-name radha-krishna-s3-bucket --template-body file://s3-bucket-template.yaml --region 'us-east-1'"
      }
    }
  }
}

